# Project Atlas

`chatterbox-docker` owns a CUDA-backed Python container. Current CI validates source syntax and repo shape without pulling the CUDA image.

## Validation

- `bash scripts/validate.sh`
